# 在扇贝里面可以添加新的单词并且修改解释
1. 首先新建自己的单词书，新建新的unit
2. 然后进入unit界面,该界面可以输入新的单词。

3. 通过编写JS文件可以实现利用函数来添加新单词以及修改释义的目的
`my_add_vocalulary(word)` 
`eg:my_add_vocalulary("hello")`:添加新单词hello

`my_edit_definition(my_word,definition)`
`eg:my_edit_definition("hello","123")`:将单词hello的释义改为123
